 ## 1. 문서 목적
> 원칙: **모든 작업은 20년차 기획/설계/개발자 기준으로 수행한다**(사용자 상시 지시). 가이드요청서가 원본 요구사항이다.
 20년차 개발/기획/설계자 기준으로 만들어줘. 내부테스트 및 내부테스트 결과서 문서로 작성해줘. 95.작업결과문서 폴더에 업무처리한 내용+YYYYMMDDHH24MISS.MD 기준으로 파일도
 만들어줘. 작성해 모든 내용의 절차적인 부분들이 들어가면 머메이드 형식으로 만들어줘.
 git add / git commit / git push / git pull 4개는 내가 직접할껍니다. 4개의 git 작업에 대하여 작업은 하지 말아주세요.

## 2. 작업 이력 요약 (2026-08-23 ~ 2026-08-24)

> 상세 이력은 `정찬성/97.작업이력/작업이력.md`, 라운드별 내부테스트 결과서는 `정찬성/95.작업결과문서/업무처리내용_YYYYMMDDHH24MISS.md`(1~4차) 참고. 여기서는 다음 세션이 빠르게 맥락을 잡을 수 있도록 핵심만 요약한다.

**무엇을 했나**: 산탄데르 고객만족 데이터 탐색 대시보드를 v1(필터 실시간 반영형)에서 **v2(업무명→모델명 선택 + `전처리조회` 버튼 트리거형)**로 전면 재구성. 근거 문서는 `정찬성/99.TRD설계서_바이브코딩/99-02_SEMI TRD_메인화면NEW20260823.md`와 `99-02_SEMI_A1_산탄데르대시보드_가이드요청서.md`.

**작업 순서(요약)**:
1. 근거 문서 2건을 정독한 결과, **문서만 있고 코드는 v1 그대로**임을 실측(TestClient로 v2 엔드포인트 4종 호출 → 전부 404)으로 확인 → No-Go 판정(1차 결과서)
2. `registry.py`/`service.py`/`ml.py`(LightGBM 실학습 ROC/AUC) 신규 작성, `crud.py`/`schemas.py`/`router.py`/프론트(`index.html`/`app.js`/`style.css`) 전면 구현 → pytest 21/21 통과, 실제 76,020행 CSV로 실측 검증(2차 결과서)
3. RandomForest 2번째 모델 추가(모델별 AUC 상이함을 완전 검증), boxplot이 극단치 때문에 안 보이던 문제를 1.5×IQR 표준 whisker 방식으로 해결, 가이드요청서 §0-1의 미확정 8개 항목 전부를 사용자와 함께 확정(특히 이상치 렌더링 방식·인증 미적용 2건은 실제 논의 후 결정) (3차 결과서)
4. 사용자가 로컬에서 직접 화면 확인 중 두 가지 실환경 이슈 발견·해결: (a) `.venv` 미활성화로 인한 `ModuleNotFoundError`, (b) **로컬 `.env` 파일 부재**로 `DATABASE_URL` 필수 필드 검증 실패 → 500 에러(`.env.example` 복사로 해결). 겸사겸사 프론트의 `Promise.all` 취약점(하나 실패 시 전체 미렌더링)도 `Promise.allSettled`로 개선
5. Render 운영 배포에서도 화면이 비어 보이는 현상 발생 → 직접 curl로 운영 API 실측한 결과 **정상 동작 확인**, 원인은 코드가 아니라 **Render 무료 티어 콜드스타트(50초+)** 타이밍 문제로 결론 → 재시도 로직(app.js) 추가 + 근본 해결로 **Render Starter(유료) 플랜 전환**(사용자가 직접 결제/설정) 완료 (4차 결과서, 최종 Go)

**핵심 교훈(다음 세션이 알아야 할 것)**:
- 이 리포지토리에서 `semi-backend`를 로컬 실행하려면 **PowerShell에서는 `.venv\Scripts\python.exe -m uvicorn ...`처럼 venv의 python을 직접 지정**하는 게 가장 안전하다(`.venv\Scripts\activate`가 확장자 문제로 안 먹는 경우가 있었음)
- `semi-backend/.env`가 없으면 dataviz의 데이터 관련 엔드포인트가 전부 500이 난다(DB를 실제로 안 쓰는데도 `DATABASE_URL`이 필수 필드라서). `.env.example`을 복사해서 쓰면 된다
- Render 무료 인스턴스 이슈처럼 "코드가 고장난 것처럼 보이지만 실제로는 배포 인프라/타이밍 문제"인 경우가 있으니, 에러 화면만 보고 코드부터 고치려 하지 말고 API를 직접 curl로 먼저 찔러볼 것
- TRD/가이드요청서에 있던 예시 수치(만족 72,928, AUC 0.8338)는 **실측과 달랐던 오기**였고, 2026-08-24에 정정 완료(현재 문서에 반영됨: 73,012 / 0.8264)