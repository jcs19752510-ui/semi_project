<!-- 네, 강제로 병합하는 방법은 있습니다. 다만 GitHub 웹에서는 안 되고 로컬 Git에서 해야 합니다.

방법 1. --allow-unrelated-histories 사용 (권장)
SEMI_PROJECT_FULL 브랜치로 이동한 후:

git checkout SEMI_PROJECT_FULL
git pull origin SEMI_PROJECT_FULL

git fetch origin

git merge origin/SEMI_PROJECT_YYH --allow-unrelated-histories
이 옵션은 공통 조상이 없는 브랜치도 병합하도록 강제합니다.

git fetch origin

git merge origin/SEMI_PROJECT_ASJ --allow-unrelated-histories
이 옵션은 공통 조상이 없는 브랜치도 병합하도록 강제합니다.

git fetch origin

git merge origin/SEMI_PROJECT_LMS --allow-unrelated-histories
이 옵션은 공통 조상이 없는 브랜치도 병합하도록 강제합니다.

git fetch origin

git merge origin/SEMI_PROJECT_JYC --allow-unrelated-histories
이 옵션은 공통 조상이 없는 브랜치도 병합하도록 강제합니다.

병합 중 충돌이 나면 해결한 뒤:

git add .
git commit
git push origin SEMI_PROJECT_FULL



원격 브랜치와 완전히 동일하게 맞추기 (강제)
git reset --hard origin/SEMI_PROJECT_FULL -->